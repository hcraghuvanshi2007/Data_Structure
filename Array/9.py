# Find the sum of the maximum and minimum elements in an array
arr = [5, 2, 8, 1, 9]

maximum = max(arr)
minimum = min(arr)

print("Sum =", maximum + minimum)

# Time Complexity: O(n) due to the single pass to find max and min
# Space Complexity: O(1)